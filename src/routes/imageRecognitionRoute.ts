import { Router } from "express";
import { analyzeSymptomImage, upload } from "../controllers/imageRecognitionController";
import { authenticate } from "../middlewares/authMiddleware";

const imageRecognitionRouter = Router();

imageRecognitionRouter.post('/image-recognition', authenticate, upload, analyzeSymptomImage)

export default imageRecognitionRouter;



























































































